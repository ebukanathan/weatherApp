// STORY
// one
// when the page opens the location of the user should be shown with the weather forcast

// the browser location can be know with the browser api
// navigator.geolocation.getCurrentPosition
// when the coordinates of the location are gotten, then it can  be to used as parameter for the forcast api

// two
// when a city is inputed in the search field that city weather forcast will be shown
// when the search city is Inputed, we will call the geocoding api, which gives the cordinate of such city
// then the cordinates can be used to get the forecsat of that city

//APi Calls
// navigator.geolocation.getCurrentPosition
// geocoding
// weather focast

// navigator

// states  ===    an object currentPosition, with properties : longitude and latitude
//  the result of the navigator will be fed to this property
//
